{
  "name": "mw-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:import-runtime": "node scripts/prepare-import-runtime.mjs",
    "predev": "npm run prepare:import-runtime",
    "dev": "wrangler dev",
    "predeploy": "npm run prepare:import-runtime",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260911.1",
    "pyodide": "314.0.6",
    "typescript": "7.0.2",
    "wrangler": "4.131.0"
  }
}
